<div class="card" style="width: 18rem;">
  <div class="card-body">
    <h5 class="card-title">{{post.title}}</h5>
    <h6 class="card-subtitle mb-2 text-muted">Posted by {{post.User.name}} on {{format_date post.date_created}}</h6>
    <p class="card-text">{{post.body}}</p>
  </div>
</div>